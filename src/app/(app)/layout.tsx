/**
 * Placeholder for the `AppShell` chrome (nav rail + terminal header + theme, per
 * docs/design-reference/DESIGN-HANDOFF.md §2). Replaced with the real shell in the UI pass.
 */
export default function AppLayout({ children }: { children: React.ReactNode }) {
  return <div>{children}</div>;
}
